1、为什么要分页？
	1）、分页可以减少网络流量，提高加载速度，提高用户体验性
	2）、较少服务器的压力
	
2、技术要点
	1）、总记录数		：查询得到的
	2）、每页记录数		：自定义的
	3）、总页数		：总页数 = (int)Math.ceil(总记录数*1.0 / 每页记录数)
	4）、偏移量		：(当前页索引 -1) * 每页记录数

3、读取指定数量的记录
	Oracle	：伪列
	Ms Sql	：Top + 子查询
	MySql	：limit 偏移数量，返回的记录数