正则表达式
	特殊字符
		()		：表示子表达式
		*		：匹配零次或多次
		+		：匹配一次或多次
		？		：非贪婪限定符，匹配前面的子表达式零次或一次
		^		：匹配表达式的开始位置，可以理解为必须以^后面的内容开始
		$		：匹配表达式结束的位置，可以理解为必须以$前面的内容结束
		|		：匹配多项中的其中一项，可以理解为或者关系
		[]		：匹配区间内的任意字符
		
	
	语法规则
		/表达式内容/参数
			参数：i -- 忽略大小写
				g -- 全局进行匹配
				m -- 多次匹配	
				
	    			